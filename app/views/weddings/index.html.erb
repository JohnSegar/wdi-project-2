<p id="notice"><%= notice %></p>

<h1 style='font-family: freight-big-pro'>Weddings</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <tbody>
    <% @weddings.each do |wedding| %>
      <tr>
        <!-- <td><%= wedding.title %></td>
        <td><%= wedding.user_id %></td> -->
        <!-- <td><%= link_to 'Show', wedding %></td>
        <td><%= link_to 'Edit', edit_wedding_path(wedding) %></td>
        <td><%= link_to 'Delete', wedding, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<div class='row'>
<%= render @weddings%>
</div>

<br>

<%= link_to 'New Wedding', new_wedding_path %>
